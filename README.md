Dependencias:

Numpy
Numba

Para poder utilizar numba:
Crear un entorno virtual:

python3 -m venv mi_entorno_proyecto
source mi_entorno_proyecto/bin/activate

Instalar numba en el entorno virtual con pip:
pip install -r requirements.txt